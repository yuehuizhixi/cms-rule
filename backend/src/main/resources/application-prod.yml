# ============================================
# 生产环境配置（Docker + Nacos）
# spring.profiles.active=prod
# Nacos 激活时走 application-common.yml，此文件仅作为本地 fallback
# ============================================
server:
  address: 0.0.0.0
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://cms-mysql.ems-pt:3306/cms_rule_db?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: ems
    password: isoengich5Ahxei
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 3
      connection-timeout: 30000

  data:
    redis:
      host: cms-redis.ems-pt
      port: 6379
      password: emsredis123
      timeout: 5000

management:
  server:
    port: 8081

cms-rule:
  iot:
    remote-url: http://cms-openapi.ems-pt:9200
  cmd:
    remote-url: http://cms-service-model.ems-pt:8200
  jwt:
    secret: ${JWT_SECRET:change-me-in-production}
  cache:
    enabled: true
  rate-limit:
    default-qps: 200
    burst-seconds: 5
